const Navbar = () => {
  return (
    <header
      style={{
        background: "#eee",
        padding: "1rem",
        borderBottom: "1px solid #ccc",
      }}
    >
      <h3>Dashboard</h3>
    </header>
  );
};

export default Navbar;
